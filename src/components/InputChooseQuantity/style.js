import { StyleSheet } from 'react-native';
import { cores } from '../../../styles/estilosGlobal';

export const InputChooseQuantityStyle = StyleSheet.create({
  campo: {
    height: 25,
    borderWidth: 1,
    borderColor: cores.escuro,
    textAlign: 'center',
  }
});
